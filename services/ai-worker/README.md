# ai-worker

Phase 1 simulated generation provider. The stable worker contract is implemented alongside the jobs module before a real model is connected.

Run from the repository root after infrastructure is available:

```shell
python services/ai-worker/run.py
```
